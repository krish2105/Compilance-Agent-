# Render blueprint for the ComplianceAgent backend.
# Deploy: Render dashboard → New → Blueprint → connect this repo, or
#         render deploys automatically from this file when the repo is linked.
services:
  - type: web
    name: complianceagent-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    plan: free            # free tier sleeps on inactivity (cold start ~30-60s)
    region: oregon
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      # Primary provider is Gemini; it AUTOMATICALLY falls back to the deterministic
      # offline provider until GEMINI_API_KEY is set in the Render dashboard — so the
      # service is live at $0 immediately, and turns "generative" the moment you add
      # a free Google AI Studio key (https://aistudio.google.com/apikey). No redeploy needed.
      - key: LLM_PROVIDER
        value: gemini
      - key: LLM_ENABLE_FALLBACK
        value: "true"
      - key: VECTOR_BACKEND
        value: memory     # zero-dependency RAG store; fits the free-tier 512MB
      - key: PYTHON_VERSION
        value: "3.11"
      - key: BACKEND_API_KEY
        value: "compliance-demo-2026"   # demo-only public gate over synthetic data (safe to publish)
      - key: CORS_ORIGINS
        value: "*"        # header-based auth (no cookies), so wildcard CORS is safe here
      - key: GEMINI_API_KEY
        sync: false       # set in the Render dashboard to activate live LLM narratives
      - key: GROQ_API_KEY
        sync: false       # optional failover provider
      # --- Observability (optional): set these to stream traces to Langfuse ---
      - key: LANGFUSE_PUBLIC_KEY
        sync: false
      - key: LANGFUSE_SECRET_KEY
        sync: false
