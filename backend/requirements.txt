# ComplianceAgent backend dependencies (pinned for Python 3.9 compatibility).
# This is the LEAN set used in Docker / Render / CI. The Regulatory-Context RAG
# defaults to a zero-dependency in-memory vector store. For the optional ChromaDB
# backend, additionally install requirements-full.txt and set VECTOR_BACKEND=chroma.
fastapi==0.115.6
uvicorn[standard]==0.30.6
sse-starlette==2.1.3
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2

# Data engine
duckdb==1.1.3
pandas==2.2.3
numpy==1.26.4

# Graph analytics + hybrid retrieval (both pure-Python, lightweight — Render-safe)
networkx==3.2.1
rank-bm25==0.2.2

# Operational store + auth (Postgres-ready via DATABASE_URL; SQLite by default)
SQLAlchemy==2.0.35
PyJWT==2.9.0

# Agent orchestration
langgraph==0.2.45
langchain-core==0.3.15

# LLM providers (primary + fallback). Optional at runtime: an offline provider keeps the app $0.
google-generativeai==0.8.3
groq==0.11.0

# Testing & lint
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.7.4
