# Frontend configuration (Vite). Copy to `.env` (cp .env.example .env).
# The API base URL of the backend. Local dev default (see backend run instructions):
VITE_API_URL=http://127.0.0.1:8099

# Must match BACKEND_API_KEY in backend/.env — sent as the X-API-Key header.
VITE_API_KEY=dev-local-key
